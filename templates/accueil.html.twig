{% include '_header.html.twig' %}


<h1> Bonjour {{ name }}</h1>

{% if category.published %}
<article style="background-color: {{ category.color }}">
    <h1>{{ category.title }}</h1>
    <p> {{ category.description }}</p>
</article>
    {% else %}
        <p> Pas d'article a lire ici</p>
{% endif %}



{%include '_footer.html.twig' %}